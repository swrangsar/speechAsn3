function signal = preEmphasize(input[y, fs] = wavread('a_pani.wav');
size = size(y);
length = size(1);
figure(100), plot(y);

for k = 1:length
    if k > 1
        y(k) = y(k) - 0.97*y(k-1);
    end
end
figure(300), plot(y);